{
  "name": "device-portal",
  "dependencies": {
    "express": "^4.18.2",
    "better-sqlite3": "^8.1.0",
    "jsonwebtoken": "^9.0.0",
    "bcrypt": "^5.1.0",
    "ajv": "^8.12.0",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "typescript": "^5.1.3",
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
  "dev": "pm2 start ecosystem.config.cjs --env development",
  "start": "pm2 start ecosystem.config.cjs --env production",
  "restart": "pm2 restart device-portal",
  "stop": "pm2 stop device-portal",
  "reload": "npm run clean && npm run build && npm run start",
  "logs": "pm2 logs device-portal",
  "delete": "pm2 delete device-portal",
  "build": "tsc",
  "clean": "rm -rf dist"
  },
  "bugs": {
	"url": "https://github.com/LucaDal/device-portal/issues"
  },
  "homepage": "https://github.com/LucaDal/device-portal#readme"
}